# Architecture

CLIVO is a single front end for many clinics. One code base and one deployment
serve a dental practice, a veterinary clinic and a physiotherapy studio, and the
differences between them are **data, not branches**. The folder layout below
exists to keep that promise legible.

## Layers

```
src/
  api/          transport — generated client + query client, nothing else
    gen/        kubb output; never edited by hand
  shared/       cross-feature building blocks with no domain knowledge
    ui/         design-system primitives (Button, Field, Modal, Badge, …)
    format/     date, money, name and document formatting
  features/     one folder per business capability
    <feature>/
      model/    pure domain logic and value objects — no React
      hooks/    react-query composition and side effects
      components/  screens and widgets
  routes/       TanStack Router file routes — thin, no logic
```

### Dependency rules

- `routes` → `features` → `shared` → `api`. Never the other way round.
- A feature may import another feature's **model**, and should avoid importing
  another feature's components. `capabilities` is the deliberate exception: every
  feature asks it what this clinic has turned on.
- `shared` knows nothing about clinics, appointments or invoices. If a component
  needs a domain type, it belongs in a feature.
- Nothing outside `api/` imports `api/gen/.kubb/*` except `shared/api-error.ts`,
  which owns the single place that knows how the backend reports failures.

### Why routes stay thin

A route file declares the path, reads its params and renders one feature
component. Keeping logic out of `routes/` means a screen can be moved, nested or
reused without touching what it does — and the generated `routeTree.gen.ts`
stays a pure artifact.

## Where variability lives

The product's three extension mechanisms each have exactly one home, so adding a
clinic type never means adding an `if` in a screen.

| Mechanism | Owner | How the UI consumes it |
| --- | --- | --- |
| **A · Modules** | `features/capabilities` | `Modules.isActive(code)` and `<ModuleGate requires={…}>`. The sidebar filters itself; panels such as dependants and insurance simply do not render. |
| **B · Record templates** | `features/encounters` | `SheetFieldControl` switches on the `fieldType` the API sends. An odontogram and a body map are the same component fed different regions. |
| **C · Parameters** | `features/capabilities` | `editorFor(accepts)` parses the constraint sentence the API returns and builds the control. The settings screen has no hard-coded parameter list. |

The rule to protect: **a new module, field type or parameter must not require a
new branch in a screen.** If you find yourself writing
`if (clinic === 'vet')`, the knowledge belongs in one of the three tables above.

## Generated code boundary

`src/api/gen/` is regenerated by `pnpm generate-api` from
`../clivo-api/openapi.json`. Treat it as build output:

- Never hand-edit it; biome is configured to skip it.
- Shape changes belong in `kubb.config.ts`. The `bigint` → `number` printer
  override lives there because the API serves `int64` identifiers as plain JSON
  numbers, so the default `bigint` types described values the transport never
  produces.
- Wrap it, don't fork it. `api/client.ts` (credentials) and `api/query-client.ts`
  (retry policy) are the only places that configure the transport.

## Conventions

- **Files** — `PascalCase.tsx` for components, `kebab-case.ts` for models and
  hooks, `use-*.ts` for hooks.
- **Domain models are classes with behaviour**, not bags of getters. `Modules`
  answers `isActive`; it does not expose its array.
- **Errors** — every mutation surfaces `messageOf(error)` in a `Callout`, and
  forms map field violations through `violationsOf(error)`.
- **Copy is Portuguese, code and docs are English.**

## Rendering mode

The API authenticates with a session cookie on a different origin, which a
server render cannot forward. The app therefore runs as an SPA
(`tanstackStart({ spa: { enabled: true } })` with `ssr: false` on the root
route), and the `_app` layout guards every authenticated screen in `beforeLoad`.

## Next steps

- Seed data: the API has no tenant or user rows yet, so the sign-in flow cannot
  be exercised end to end.
- `POST /api/session` answers `500` for unknown credentials; it should be `401`
  so the login screen can explain the failure.
- Screens still to build: session packages, inventory, commissions and
  notifications — all module-gated, all already routed from the sidebar.
