/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

/**
 * @description One condition recorded on one region of a special component. Within a single field value, no region and part may be marked twice: `{\"region\":\"26\",\"parts\":[\"oclusal\"]}` and a second marking of `26`/`oclusal` are refused together, whatever their marks. Two markings on different parts of the same region are accepted.
 * @type object
*/
export type RegionMarking = {
    /**
     * @description Code of the condition, from ComponentDescriptor.vocabulary[].code.
     * @type string | undefined
    */
    mark?: string;
    /**
     * @description Free note the practitioner wrote about this marking.
     * @maxLength 400
     * @type string | undefined
    */
    note?: string;
    /**
     * @description Parts of the region this condition covers, each from the region\'s own `parts`. Empty or absent marks the region as a whole. The vocabulary entry decides which is allowed: `appliesTo` REGION demands it be empty, PART demands at least one, ANY accepts both. Several parts in one marking are one clinical fact sharing one note; \"every part\" is the explicit full list, never a wildcard.
     * @type array | undefined
    */
    parts?: string[];
    /**
     * @description Code of the marked region, from ComponentDescriptor.regions[].code.
     * @type string | undefined
    */
    region?: string;
};
